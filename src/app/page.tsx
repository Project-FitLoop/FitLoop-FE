import React from 'react';
import LoadingWrapper from '@/components/loadingWrapper';

const MainPage = () => {
  return (
    <div>
      <LoadingWrapper />
    </div>
  );
};

export default MainPage;